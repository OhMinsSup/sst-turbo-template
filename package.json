{
  "name": "template-app",
  "version": "0.0.1-beta.0",
  "private": true,
  "description": "template",
  "keywords": [
    "nextjs"
  ],
  "license": "ISC",
  "author": "OhMinsSup",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*"
  ],
  "scripts": {
    "build": "dotenv -- turbo run build",
    "build:env": "turbo run build:env",
    "build:libs": "turbo run build:libs",
    "clean": "turbo clean",
    "clean:node_modules": "find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +",
    "predev": "env-cli --env-type local",
    "dev": "dotenv -- turbo run dev --concurrency 255",
    "dev:admin": "dotenv -- pnpm run dev --filter @template/admin",
    "dev:web": "dotenv -- pnpm run dev --filter @template/web",
    "eli": "env-cli",
    "fix:eslint": "turbo run fix:eslint",
    "fix:prettier": "pnpm lint:prettier --write",
    "lint": "turbo run lint",
    "lint:prettier": "prettier --check --ignore-unknown '**/*'",
    "lint:spellcheck": "cspell --no-progress --relative '**/*'",
    "lint:syncpack": "syncpack lint",
    "lint:typecheck": "turbo run lint:typecheck"
  },
  "prettier": "@tooling/prettier-config/web",
  "dependencies": {
    "@turbo/gen": "^1.12.5"
  },
  "devDependencies": {
    "@template/env": "workspace:*",
    "@tooling/prettier-config": "workspace:*",
    "@tooling/tsconfig": "workspace:*",
    "cspell": "^8.6.0",
    "dotenv-cli": "^7.4.1",
    "prettier": "^3.2.5",
    "syncpack": "^12.3.0",
    "turbo": "^1.12.5",
    "typescript": "^5.4.2"
  },
  "packageManager": "pnpm@8.12.1",
  "engines": {
    "bun": ">=1.0",
    "node": ">=18.0"
  }
}
